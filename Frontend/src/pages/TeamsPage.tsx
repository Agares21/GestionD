import React from "react";
import { Layout } from "@components/layout";
import { TeamList } from "@components/index";

const TeamsPage: React.FC = () => {
  return (
    <Layout>
      <TeamList />
    </Layout>
  );
};

export default TeamsPage;
